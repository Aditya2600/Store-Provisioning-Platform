apiVersion: v1
kind: ConfigMap
metadata:
  name: dashboard-runtime-config
data:
  config.js: |
    window.__APP_CONFIG__ = {
      API_BASE: "{{ default (printf "%s://api.%s" .Values.urlScheme .Values.baseDomain) .Values.dashboard.apiBase }}"
    };
